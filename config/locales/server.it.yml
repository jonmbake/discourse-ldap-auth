it:
  site_settings:
    ldap_enabled: "É il plugin LDAP attivato?"
    ldap_user_create_mode: "Modalità di creazione degli utenti:: auto, list o none"
    ldap_hostname: "Hostname del server LDAP"
    ldap_port: "Porta del server LDAP"
    ldap_method: "Metodo di connessione: ssl, tls o plain"
    ldap_base: "Base LDAP"
    ldap_uid: "LDAP uid"
    ldap_bind_db: "LDAP bind db (necessaria se il server LDAP non consente bind anonimo)"
    ldap_password:  "LDAP bind password (necessaria se il server LDAP non consente bind anonimo)"
    ldap_filter:  "Filtro LDAP (per specificare le autorizzazioni per i grupi, etc.)"
