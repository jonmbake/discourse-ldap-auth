pt_BR:
  site_settings:
    ldap_enabled: "O plugin LDAP está ativo?"
    ldap_hostname: "Hostname do servidor LDAP"
    ldap_port: "Porta de conexão do servidor LDAP"
    ldap_method: "Método da conexão:: ssl, tls ou plain"
    ldap_base: "LDAP base"
    ldap_uid: "LDAP uid"
    ldap_bind_db: "Usuário LDAP bind (necessário se o servidor LDAP não permite binding anônimo)"
    ldap_password:  "Senha LDAP bind (necessário se o servidor LDAP não permite binding anônimo)"
